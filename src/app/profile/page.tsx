'use client';

import UserProfile from "@/components/page/UserProfile/UserProfile";

const page = () => {
    return (
        <div>
            <UserProfile />
        </div>
    );
};

export default page;