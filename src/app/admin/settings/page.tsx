'use client';

import Settings from "@/components/page/admin/Settings/Settings";

const page = () => {
    return (
        <div>
            <Settings />
        </div>
    );
};

export default page;